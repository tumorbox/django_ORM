from django.shortcuts import render, redirect
import requests

# 모델 가져오기
from jobs.models import Person

# Create your views here.
def index(requests):
    return render

def past_life(requests):
    return render
